## **題目 1：建立 Next.js 專案並運行提供的 `page.tsx`**

### **目標**

- 從頭建立一個 Next.js 專案（建議使用 TypeScript 支援）。
- 將我們提供的 `example/TodoList.tsx` 程式碼納入專案，並確保頁面能夠在瀏覽器中正常顯示。
- 本題目中的 `example/TodoList.tsx` 使用`Tailwind CSS`來進行樣式設計，可以查閱相關說明文件來了解如何使用。
- 本題目著重於測驗面試者是否能針對沒見過的新技術有良好的學習能力。
- 面試者可以在測驗進行中查閱相關文件，並且不限制使用任何線上資源包含ChatGPT、Claude、DeepSeek等。

### **要求**

1. **環境搭建：**

   - 請從頭建立一個 Next.js 專案。你可以使用上網查詢官方提供的指令。
   - 確保專案能夠正確啟動（例如使用 `npm run dev` 或 `yarn dev`）。

2. **主頁搭建：**
   - 將我們提供的 `example/TodoList.tsx` 程式碼納入專案。
   - 複製或引入 `example/TodoList.tsx`中的內容到首頁的`page.tsx`作為首頁內容。

3. **驗證運行：**
   - 確保 `localhost:3000` 能夠正確顯示 `example/TodoList.tsx` 中的內容。

4. **樣式設計：**
    - 使用 `Tailwind CSS` 來設計 `example/TodoList.tsx` 中的樣式。
    - 可以參考 `example/TodoList.tsx` 中的樣式設計，或自行設計樣式。
    - 要求：文字不能擠在一起，有適當的間距，適當的排版即可，不需過度設計，如有疑問可以直接問在場面試官。

---

## **任務 2：擴充 TODO List 功能（新增與刪除）**

### **目標**

在已建立的 Next.js 專案中，對首頁進行修改，實現基本的 TODO list 功能，並解決可能存在的程式碼問題。

### **要求**

1. **新增 TODO 項目：**

   - 提供一個輸入框讓使用者輸入新的 TODO 項目。
   - 完成`handleAddTodo`，使得當使用者點擊「Add」按鈕後，該項目能正確加入列表中。

2. **刪除 TODO 項目：**

   - 為每個 TODO 項目增加一個「刪除」按鈕，使用者點擊後，能夠從列表中移除該項目。  
     **提示：** 建議將每個 TODO 項目從原本的單純字串轉換為物件結構，並且保持id是unique的，例如：
     ```ts
     { id: string, content: string }
     ```
     這樣可以更容易地管理每個項目的唯一性，避免使用 index 作為 key 所產生的問題。

3. **UI 與錯誤處理：**

   - 請確保列表在新增或刪除後能夠即時更新並正確渲染。
   - 遇到錯誤或空值時，請給予使用者友善提示。

---

## **任務 3 (Bonus)：實現 TODO 項目細節頁面（動態路由）**

### **目標**

- 當使用者點擊某個 TODO 項目時，透過 Next.js 動態路由機制跳轉到該項目的詳細頁面，並展示該 TODO 項目的基本資訊。

- 確保不同頁面中，能夠使用相同的data source，不會出現資料不一致或難看的假資料

### **提示**

- 可以查詢nextjs官方文件中的[動態路由](https://nextjs.org/docs/routing/dynamic-routes)部分，了解如何實現動態路由。

- 這裡提供一個不同頁面中，能夠使用相同的data source的解決方案：
    - 可使用`Prisma`或其他資料庫作為資料庫，替換掉初始頁面的假資料(`useState(["Buy milk", "Walk dog", "Read book"]);`)，並且使用API Route來提供資料庫中的資料給前端。
    - 或是你認為有更簡潔的方案，比如`React Context`、`Redux`也可以使用。

### **要求**

1. **動態路由頁面：**

   - 建立一個新頁面檔案 `pages/todo/[id].tsx`。
   - 使用 Next.js 提供的動態路由（Dynamic Routing）功能，根據 URL 中的 `id` 參數展示對應的 TODO 項目內容。

2. **頁面內容：**

   - 在詳細頁面中，根據 `id` 讀取並展示 TODO 項目的資訊（例如：內容、創建時間、其他相關資訊）。
   - 若 URL 中的 `id` 無對應的項目，請顯示適當的錯誤訊息或提示使用者「項目不存在」。

3. **導航：**
   - 請在首頁 TODO list 的每個項目上，使用 Next.js 的 `Link` 組件包裹，實現點擊項目後能夠導航到對應的 `/todo/[id]` 頁面。

---

## **評分標準**

| **項目**               | **評估重點**                                                                           |
| ---------------------- | -------------------------------------------------------------------------------------- |
| **環境搭建能力**       | 能否從頭建立一個 Next.js 專案並正確運行提供的 `page.tsx`                               |
| **問題識別與解決能力** | 能否發現原有程式碼中的潛在問題，並提出合理的修正方案                                   |
| **功能實現**           | 新增、刪除、動態路由導航以及詳細頁面是否能夠正確運作，使用者體驗是否良好               |
| **程式碼品質**         | 程式碼結構是否清晰、元件拆分是否合理、變數命名是否有意義、是否包含必要的註解及錯誤處理 |
| **使用者體驗**         | UI 設計是否直覺、互動是否流暢、錯誤提示是否友好、頁面美感是否符合現代化設計標準        |

---

希望這份挑戰能夠全面展現你在前端技術、問題解決、以及使用者體驗設計方面的能力。祝你挑戰順利！
